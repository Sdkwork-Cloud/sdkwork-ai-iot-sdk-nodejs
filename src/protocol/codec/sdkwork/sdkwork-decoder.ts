import { XiaozhiProtocolDecoder } from '../xiaozhi';
import { ResponseProtocol } from '../../../types/protocol';

// Implementation class
export class SDKWorkProtocolDecoderImpl extends XiaozhiProtocolDecoder {
  decode(content: string): ResponseProtocol {
    // Empty implementation, to be implemented later
    throw new Error('Method not implemented.');
  }
}
